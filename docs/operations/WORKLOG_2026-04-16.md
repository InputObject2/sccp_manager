# Worklog 2026-04-16

## Scope
- SCCP extension tab persistence bug (`issue #1`)
- Follow-up voicemail related behavior (`*97`)
- SCCP core driver field mapping (`vmnum`, `trnsfvm`)

## Code Changes
1. `078ea8b` - fixed SCCP tab persistence for extension values (`devinfo_*` save/read path).
2. `6044fb6` - fixed SCCP core driver mapping to persist:
- `vmnum`
- `trnsfvm`

## Validation Summary
1. Reproduced old behavior on stand before sync.
2. Synced stand to `origin/develop`.
3. Verified persistence for SCCP line fields after save.
4. Ran integration checks through FreePBX Core/SCCP driver path:
- create flow: values written to `sccpline`
- update flow: changed values persisted on repeat save
5. Cleaned test records (`6998`, `6999`) from `sccpline`, `devices`, `users`.

## GitHub Actions
1. Added issue updates with current status and commit references.
2. Reopened `issue #1` for continued tracking until reporter confirmation.

